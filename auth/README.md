# SQLITE CENTRALIZED ACTIONS

BROAD STROKES simple API
Meant for server use that _does not expose any table creation apis_
and creates semantic _actions_ for developers to do:


auth
    rate_limit_ip_address
    create_guest_session
    request_account
    register_account
    login
    rate_limit_session
    logout
    request_contact
    confirm_contact

